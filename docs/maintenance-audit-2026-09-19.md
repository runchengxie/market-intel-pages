# 2026 年 9 月 19 日维护检查

## 范围和步骤

本次检查 `market-intel-pages` 的代码、数据、文档、Git 状态和 GitHub Pages 发布。仓库没有 submodule。`quant-intel-platform` 负责报告生成，`quant-intel-deploy` 负责服务器调度，它们是相邻的独立仓库。

用户要求处理可安全合并的历史增量、清理已合并分支与残留工作树、检查生产和 CI、更新数据，并改善文档与代码维护方式。

- [x] 核对本地和远端分支、PR、issue、stash 与残留工作树。
- [x] 对照历史提交内容，清理已合并且没有独有文件的遗留引用。
- [x] 核对原始行情、报告入口和生产版本。
- [x] 增加可重复执行的公开报告准备入口，补齐 9 月 15 日至 18 日晚报和 18 日晨报。
- [x] 区分行情日期、实际生成时间和补发记录，保留旧观点与后续结果的独立记录。
- [x] 改善类型检查、复杂度、覆盖率、依赖审计及 CI。
- [x] 改写中文说明文档，保留必要历史记录，删除过期操作指引。
- [ ] 完成评审、PR、部署验收和本轮工作树清理。

## Git 与 GitHub

检查开始时，主分支位于 `7745568`，工作区干净，与 `origin/main` 一致。服务端只有 `main` 分支，PR 1 至 9 均已合并，没有 open issue，也没有 stash。

| 遗留项 | 核对依据 | 处理 |
| --- | --- | --- |
| `docs/quant-market-intel-pages-summary-design` | `f53025a` 是 main 的祖先，领先 0 个提交 | 已删除本地分支 |
| 旧 `/tmp/codex-market-intel-pages-summary-design` 工作树 | 目录已不存在，残留 index 与对应提交没有差异 | 已清理 Git 管理记录 |
| `origin/pr-5` 本地引用 | 与 PR 5 的 squash 提交 `6dc8bb2` 文件树一致，远端不存在同名分支 | 已删除本地引用 |
| 4 个悬空提交 | 分别与 PR 6 至 9 的合并结果文件树一致 | 保留给 Git 常规垃圾回收 |

Windows 上的旧临时复制目录没有可用的 Git 元数据。其 35 个受控文件与主分支内容一致，另有 18 个 Python 缓存文件，没有需要恢复的增量。

历史 Actions 失败已读取日志核实。9 月 15 日首次上传把多个文件名当成单一目录，PR 2 已修复。9 月 16 日构建目录位于仓库内，被路径保护拒绝，PR 6 已修复。后续主分支发布成功，无需重跑旧提交。

## 数据与生产链

原始 A 股行情已到 9 月 18 日，网站报告停留在 9 月 14 日。服务器使用现有交易日数据生产 CLI 可以生成 18 日的晨晚报。

发现的问题：

- 原有晨报清单为空，晨报流水线直接把标准输出重定向到正式清单。命令失败会先清空原文件。
- 恢复任务显式停用旧 `morning_model`，晨报仍依赖这个阶段，因此也被停用。
- 9 月 19 日的晚报恢复状态为 `waiting_source_window`，不能仅凭定时器还在运行就认定报告已产出。
- 旧报告今天补发时，实际生成时间会变新。新鲜度检查必须同时看目标行情日期，解读也必须继承补发标记。
- 晨报依赖同日期跨市场快照。历史日期缺快照时，不能用今天的实时查询冒充当时材料。

报告准备入口只调用已安装的公开 CLI，输出到仓库外。校验通过后生成明确允许公开的清单，再由 `import_reports.py` 导入。原始 JSON、日志、历史修订和模型记录留在私有归档中。

```mermaid
flowchart LR
    A[权威行情与跨市场快照] --> B[quant-intel-platform 公开 CLI]
    B --> C[仓库外暂存与来源校验]
    C --> D[公开 Markdown 清单]
    D --> E[import_reports]
    E --> F[最近五个日期的报告]
    E --> G[私有完整归档]
    F --> H[Gemini 解读与引用校验]
    H --> I[观点版本与后续条件核对]
    F --> J[Pages 构建与发布]
    I --> J
```

检查时网站生产版本与 `main` 一致。相邻生产仓库的版本记录如下，这些版本差异需要按各仓发布流程处理。

| 仓库 | production current | origin/main | 差异 |
| --- | --- | --- | --- |
| `quant-intel-platform` | `06bf665` | `3f1810a` | 配置路径检查和文档更新 |
| `quant-intel-deploy` | `4fa410d` | `3c32b76` | 配置目录迁移、保留 TuShare 扩展依赖和文档更新 |

两个相邻仓库的主检出都有既有未提交修改。本轮通过独立工作树处理网站，不覆盖这些修改。

## 代码质量基线

基线为 9 个生产 Python 脚本，共 1213 行，运行时仅使用标准库。原有 45 项 Python 测试和 6 项 JavaScript 测试通过。

Python 行与分支联合覆盖率约为 75%。类型检查发现 10 处问题。48 个函数和类的平均 McCabe 复杂度为 8.56，最高的函数分别为解读生成 40、报告导入 32、解读结构校验 26。

整改关注职责拆分、数据边界和失败路径。指标用于定位难维护的代码，不通过删测试、扩大忽略规则或机械拆碎函数来降低数字。

重构后保留标准库运行时，没有新增第三方运行依赖。新增的报告准备模块只通过 CLI 调用上游，原有模块之间仍是 13 条静态导入边，没有增加业务抽象层。没有巨型类，也没有发现达到 Vulture 80% 置信度的未使用代码，因此未为减少行数删除仍在使用的入口。

| 指标 | 整改前 | 本轮检查结果 |
| --- | --- | --- |
| Python 测试 | 45 项 | 88 项及 20 个子用例 |
| 行与分支联合覆盖率 | 74.92% | 90.09%，门槛 85% |
| Radon 最大圈复杂度 | 40 | 21 |
| Radon 平均圈复杂度 | 8.56 | 约 6.4 |
| Complexipy 最大认知复杂度 | 48 | 27 |
| Complexipy 平均认知复杂度 | 9.63 | 约 6.2 |
| 生产 Python 模块 | 9 | 10，新增报告准备入口 |
| 生产 Python LOC | 1213 | 约 1960 |
| 静态本地导入边 | 13 | 13 |
| 第三方运行依赖 | 0 | 0 |
| Ruff 忽略规则 | 无统一配置 | 无新增忽略项，McCabe 上限 10 |

LOC 增长来自统一格式、职责拆分、错误处理和新增报告准备流程，不能把它解释为代码变得更简洁。认知复杂度使用 Complexipy 8.0.1 同口径比较。最高值仍在服务错误提取与简评生成函数，后续应结合真实修改频率继续收敛，避免只为分数拆出过多函数。

`tools/audit_structure.py` 输出每个模块的 LOC、AST 节点数、本地导入边、标准库依赖和按名称可解析的直接调用边。它不追踪动态分派、方法调用和注入函数。CI 保存该报告、覆盖率 XML 和依赖审计 JSON，供以后比较。完整操作命令见 README。

生产依赖链按不可变提交记录：行情与日历由数据平台提供，报告能力由公开框架 CLI 提供，私有部署固定公开框架版本，网站由自身 PR 和 Actions 发布。报告目标日期、原始生成时间、补发时间、模型版本、提示词哈希、来源哈希与部署回执各自保留，便于追踪一个页面观点所用的材料。

## 数据补发结果与限制

9 月 19 日使用服务器真实行情重建 15 日至 18 日晚报及 18 日晨报。公开快照共七份报告，覆盖 14 日至 18 日五个报告日期。18 日晨晚报配对成功，显式传入交易日历目标 18 日的健康检查通过。

所有新增报告标为补发，生成时间为本次实际运行时间。15 日至 17 日的指数数据缺失、部分日期的资金流与两融缺项原样说明。18 日晨报使用同日期跨市场快照，新闻输入禁用。A 股数据健康回执允许发布，但仍有十条非阻断质量提醒，不能据此宣称所有上游数据完整。

## 生产验收记录

网站代码发布、私有定时器启用和线上闭环验收分别留存回执。生产调度的最终状态以部署侧运行手册和服务器成功回执为准，代码具备入口不等于定时任务已经启用。
